spring.application.name=animalservice

spring.datasource.url=jdbc:mariadb://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Should create a key using:
# private key: openssl genpkey -algorithm RSA -out priv.pem -pkeyopt rsa_keygen_bits:2048
# openssl rsa -pubout -in priv.pem -out pub.pem
# Should maybe be changed
jwt.public.key.path=/app/src/main/resources/security/pub.pem